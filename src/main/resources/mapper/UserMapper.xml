<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kopo.poly.kpaas.mapper.UserMapper">

    <select id="getUserByEmail" parameterType="string" resultType="kopo.poly.kpaas.dto.UserDTO">
        SELECT
            user_id      AS userId,
            name         AS userName,
            email        AS userEmail,
            nickname     AS userNickName,
            password     AS userPassword,
            birth_date   AS userBirthDate,
            is_foreigner AS isForeigner,
            created_at   AS createdAt,
            email_code   AS authCode,
            limited_time AS limitedTime
        FROM users
        WHERE email = #{userEmail}
    </select>

    <update id="updatePasswordByEmail">
        UPDATE users
        SET password = #{userPassword}
        WHERE email = #{userEmail}
    </update>

    <update id="updateEmailCodeByEmail">
        UPDATE users
        SET email_code = #{authCode},
            limited_time = #{limitedTime}
        WHERE email = #{email}
    </update>


</mapper>
